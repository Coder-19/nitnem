import 'package:flutter/material.dart';

// the below constant contains the code about the design of the page numbers
// to be displayed at the top of each path
const kPageNumberTextDesign = TextStyle(
  fontSize: 23.0,
//  fontWeight: FontWeight.bold,
  color: Colors.white,
  // color: Colors.brown,
);

// creating a constant for the bottom container color
const kBottomContainerColor = BoxDecoration(
//  color: Colors.yellow,
  //color: Color(0xFFC18C1E),
  color: Color(0xFFC46210),
);

// the below constant is for the color present in the container that displays the
// page numbers
const kUpperContainerColor = BoxDecoration(
  color: Color(0xFFC46210),
  // color: Colors.amber,
  // color: Color(0xFFFFCC00),
  // color: Colors.orange,
);
