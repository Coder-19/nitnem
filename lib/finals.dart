import 'package:flutter/material.dart';


// the final below named imageShow is used to display the specified image
final imageShow = Image.asset('images/nishanSahib.png');

// the final below is used to display the background color of the appBar
final appBarBackgroundColor = Colors.yellow[700];

// the final used below is used to display the background color of the container
final containerBackgroundColor = BoxDecoration(
  gradient: LinearGradient(
    colors: [Colors.yellow[700], Colors.white, Colors.yellow[700],],
  ),
);








